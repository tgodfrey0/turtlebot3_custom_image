# Copy to .env and fill in (or export directly). .env is gitignored.
# Tailscale auth key - generated at https://login.tailscale.com/admin/settings/keys
# Takes precedence over [tailscale] auth_key in config files.
TAILSCALE_AUTH_KEY=<your-tskey-goes-here>